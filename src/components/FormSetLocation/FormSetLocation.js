import React from 'react';
import FormSetLocationStyle from './_formSetLocation.scss'
import { Link, Route, Switch } from 'react-router-dom';
import PlacesAutocomplete from 'react-places-autocomplete';


class FormSetLocation extends React.Component {
    constructor(props) {
        super(props);
    //    this.handleFormSubmit = this.handleFormSubmit.bind(this);
     //   this.handleOnChange = this.handleOnChange.bind(this);
    }

   /* handleFormSubmit(event) {
        event.preventDefault()
        console.log("event es: " + event.target);
        this.props.formSubmit();
    }
    handleOnChange() {
      //  console.log("event es: " + event.target.value);
        this.props.onChange();
    } */

    render() {
        return (
            <div>


      

            </div>
        );
    }
};

export default FormSetLocation;  